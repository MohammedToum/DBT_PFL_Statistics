[sqlfluff]
dialect = duckdb
templater = dbt
max_line_length = 120
exclude_rules = ST09,LT09  # relax for speed; re-enable later

[sqlfluff:templater:dbt]
# Point to your dbt project root if needed; often not required when running from repo root.
project_dir = .
profiles_dir = ./dbt